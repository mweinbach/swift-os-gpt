# SwiftOS Raspberry Pi 5 research target. Hardware support is not yet verified.
[pi5]
arm_64bit=1
kernel=kernel8.img
device_tree=bcm2712-rpi-5-b.dtb

# SwiftOS is bare metal, so skip firmware Linux compatibility checks.
os_check=0

# Keep the firmware-expanded DTB outside both the 32 MiB live-update
# destination and the >=64 MiB upload workspace. The kernel independently
# validates and reserves the actual x0 span before enabling activation.
device_tree_address=0x03000000
device_tree_end=0x03200000

# RP1 is attached through the Pi 5's internal PCIe x4 controller. SwiftOS does
# not yet enumerate/configure that root complex, so retain the BAR/range setup
# established by the bootloader instead of resetting it before kernel entry.
pciex4_reset=0

# Route the USB-C data pair to the SoC DWC2 controller in device mode. This is
# the physical debug transport; endpoint protocol and controller setup remain
# SwiftOS driver responsibilities.
dtoverlay=dwc2,dr_mode=peripheral

# Pi 5 defaults to leaving mode selection to an OS KMS driver. SwiftOS does
# not own HVS/HDMI modesetting yet, so ask firmware to read HDMI EDID, select
# the attached display's mode, and retain a 32-bit simple-framebuffer handoff.
disable_fw_kms_setup=0
framebuffer_depth=32
framebuffer_ignore_alpha=1

# UART10 is the preferred early console on the dedicated debug connector.
enable_uart=1
uart_2ndstage=1

# Emit hashes of firmware-loaded files in the second-stage UART log.
sha256=1

[all]
